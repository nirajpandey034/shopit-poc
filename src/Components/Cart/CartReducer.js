export let initialState = {
  products: [
    { name: "apple" },
    { name: "orange" },
    { name: "pineapple" },
    { name: "mango" },
  ],
};
export const CartReducer = ({ state, action }) => {
  return;
};
